# MongoDB
MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/examguard

# JWT
JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=60m
JWT_REMEMBER_EXPIRES_IN=7d

# Email (Gmail App Password)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=muhammadumair19925@gmail.com
EMAIL_PASS=sarbwecxxzmvgvav

# Frontend origin (comma-separated for multiple, e.g. preview deployments)
CLIENT_URL=http://localhost:5173

# App
PORT=5000
NODE_ENV=development
